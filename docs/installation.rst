============
Installation
============

At the command line::

    $ easy_install django-bank

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-bank
    $ pip install django-bank
